The regularization paths show a clear difference between L1 and L2 on this telecom churn dataset. As regularization becomes stronger (smaller C), L1 pushes several coefficients exactly to zero, with has_dependents, senior_citizen, payment_method_Electronic check disappearing earliest, which suggests that these features are weaker or more redundant once other predictors are present. In contrast, contract_type_Two year, contract_type_One year, tenure remain comparatively more stable across the regularization spectrum, making them likely to be the most robust churn predictors in the dataset. L2 shrinks coefficients continuously toward zero but rarely eliminates them completely, so it preserves more signal from correlated features while reducing variance. Based on this path, L2 is the safer production choice, because it shrinks coefficients smoothly while preserving information across correlated predictors instead of aggressively removing them.
